# Dual-Runtime Claude Code Plugin — Design Specification

## Summary

Transform `maestro-gemini` into `maestro-orchestrate` — a dual-runtime (Gemini CLI + Claude Code) multi-agent orchestration platform with its own MCP server source, no dependency on overture. Three phases, each independently shippable with a clean rollback path.

## Context

**Maestro** is a multi-agent orchestration platform currently shipping as a Gemini CLI extension. It coordinates 22 specialized subagents through two workflows: Express (simple tasks) and Standard 4-phase (medium/complex tasks). The platform was originally generated by **overture**, a multi-runtime generator framework that transforms canonical agent definitions into runtime-specific packages for Gemini CLI, Claude Code, and Codex.

Overture already generates a complete Claude Code plugin (`dist/claude-plugin/`) from canonical content, but the generator has proven to be an unnecessary layer of indirection. This spec defines the path to:

1. Bring the Claude Code plugin into this repo as a first-class runtime
2. Migrate the MCP server source from overture
3. Replace overture's 688-line generator with a lightweight in-repo script
4. Rebrand from `maestro-gemini` to `maestro-orchestrate`
5. Decommission overture

**Overture reference commit**: `b314d9d` (the version that generated the current maestro-gemini content).

## Constraints

- All work happens within this repository on the `main` branch
- No symlinks — must work across all OS and policy configurations
- Each phase is independently shippable and rollback-safe
- Existing Gemini CLI functionality must not regress at any point
- The user-facing extension/plugin name stays `maestro` on both platforms

## Cross-Cutting Concerns

### Claude Code Hook I/O Contract

Claude Code hooks use a different I/O contract than Gemini hooks. The `hook-adapter.js` in `claude/scripts/` must normalize between these:

| Aspect | Gemini CLI | Claude Code |
|--------|-----------|-------------|
| Hook events | `SessionStart`, `BeforeAgent`, `AfterAgent`, `SessionEnd` | `SessionStart`, `SessionEnd`, `PreToolUse`, `PostToolUse` |
| PreToolUse input | N/A | `{ tool_name, tool_input }` via stdin JSON |
| Denial signaling | `{ continue: false, systemMessage: "..." }` | `process.exit(2)` + `permissionDecision: "deny"` in stdout JSON |
| Path variable | `${extensionPath}` | `${CLAUDE_PLUGIN_ROOT}` |
| Agent detection | `BeforeAgent` event with `prompt` field | `PreToolUse` event with matcher `Agent`, `tool_input` contains agent prompt |

Overture's generated `hook-adapter.js` and `policy-enforcer.js` (commit `b314d9d`) implement the Claude-specific contract: `policy-enforcer.js` uses `process.exit(2)` for deny decisions and reads `tool_input.command` for Bash tool calls. These files are copied as-is in Phase A. Functional validation against a live Claude Code session is a Phase A acceptance criterion.

### Environment Variable Resolution

Gemini CLI sets `MAESTRO_WORKSPACE_PATH` (via `gemini-extension.json` mcpServers env) and makes `${extensionPath}` available. Claude Code provides `${CLAUDE_PLUGIN_ROOT}` and `${CLAUDE_PROJECT_DIR}` instead. The `lib/config/setting-resolver.js` and `lib/core/project-root-resolver.js` read Gemini-specific env vars.

The **root** `lib/` files must be patched to also check `CLAUDE_PLUGIN_ROOT` (as fallback for `MAESTRO_EXTENSION_PATH`) and `CLAUDE_PROJECT_DIR` (as fallback for `MAESTRO_WORKSPACE_PATH`). This is a targeted 1-line change per file (adding `|| process.env.CLAUDE_PLUGIN_ROOT` to the existing env var read). Patching root `lib/` rather than `claude/lib/` ensures the MCP server bundle (built from root `lib/` via esbuild) also picks up the fallbacks. The additional env var checks are harmless under Gemini CLI — they are always undefined. Phase A must include these patches before copying `lib/` into `claude/lib/`.

---

## Phase A: Claude Code Static Integration

### Objective

Add Claude Code plugin artifacts to this repo as static files, colocated with existing Gemini extension files. Zero modifications to existing files. Purely additive.

### Core Problem

Both Gemini CLI and Claude Code discover agents from `agents/*.md` and skills from `skills/*/SKILL.md` relative to their extension/plugin root. The two runtimes require incompatible content:

| Difference | Gemini CLI | Claude Code |
|------------|-----------|-------------|
| Agent frontmatter | `kind: local`, `temperature`, `max_turns`, `timeout_mins` | `model: inherit`, `color`, example triggers in description |
| Tool names | `read_file`, `write_file`, `run_shell_command`, `ask_user` | `Read`, `Write`, `Bash`, `AskUserQuestion` |
| Agent filenames | `snake_case` (`code_reviewer.md`) | `kebab-case` (`code-reviewer.md`) |
| Command format | TOML (`commands/maestro/*.toml`) | Markdown (`commands/*.md` with `allowed-tools` frontmatter) |
| Hook schema | Gemini hook events (`SessionStart`, `BeforeAgent`, `AfterAgent`) | Claude hook events (`SessionStart`, `SessionEnd`, `PreToolUse`) |
| Orchestrator | `GEMINI.md` context file | Embedded in command Markdown + command-backed skill |
| Policies | `policies/maestro.toml` (Gemini policy engine) | `scripts/policy-enforcer.js` (PreToolUse hook) |

If both runtimes point to the same root, they discover each other's incompatible content.

### Solution: Nested Plugin Root

Repo root serves as the Gemini extension root (unchanged). A `claude/` subdirectory serves as the Claude Code plugin root. Each runtime discovers only its own content.

Shared resources (`lib/`, `mcp/`, `templates/`, `references/`) are **fully copied** into `claude/` — no symlinks. This duplication is intentional and temporary; Phase C eliminates it.

### Directory Layout

```
maestro-gemini/                          # Gemini extension root (ALL UNCHANGED)
├── gemini-extension.json
├── GEMINI.md
├── agents/                              # Gemini agents (22 files, snake_case)
├── commands/maestro/*.toml              # Gemini commands (12 TOML files)
├── skills/                              # Gemini skills (7 core skills)
├── hooks/hooks.json                     # Gemini hooks
├── policies/maestro.toml               # Gemini policies
├── lib/                                 # Shared library (14 files)
├── mcp/maestro-server.js               # Bundled MCP server
├── scripts/                             # Gemini hook entry points
├── templates/                           # Document templates (3 files)
├── references/                          # Architecture docs (1 file)
│
└── claude/                              # Claude Code plugin root
    ├── .claude-plugin/
    │   └── plugin.json
    ├── agents/                          # 22 agents (kebab-case, Claude frontmatter)
    ├── commands/                        # 12 commands (Markdown + allowed-tools)
    ├── skills/                          # 19 skills (7 core + 12 command-backed)
    │   ├── code-review/SKILL.md
    │   ├── delegation/
    │   │   ├── SKILL.md
    │   │   └── protocols/
    │   │       ├── agent-base-protocol.md
    │   │       └── filesystem-safety-protocol.md
    │   ├── design-dialogue/SKILL.md
    │   ├── execution/SKILL.md
    │   ├── implementation-planning/SKILL.md
    │   ├── session-management/SKILL.md
    │   ├── validation/SKILL.md
    │   └── maestro-*/SKILL.md           # 12 command-backed skills
    ├── hooks/
    │   └── hooks.json                   # Claude hooks (SessionStart, SessionEnd, PreToolUse)
    ├── scripts/                         # Claude hook entry points (322 lines total)
    │   ├── session-start.js
    │   ├── session-end.js
    │   ├── before-agent.js
    │   ├── hook-adapter.js
    │   ├── policy-enforcer.js
    │   ├── ensure-workspace.js
    │   ├── read-active-session.js
    │   ├── read-setting.js
    │   ├── read-state.js
    │   └── write-state.js
    ├── lib/                             # Full copy of lib/ (no symlink)
    ├── mcp/                             # Full copy of mcp/ (no symlink)
    ├── templates/                       # Full copy of templates/
    ├── references/                      # Full copy of references/
    ├── mcp-config.example.json
    └── package.json
```

### Collision Analysis

| Resource | Gemini Location | Claude Location | Collision |
|----------|----------------|-----------------|-----------|
| Manifest | `gemini-extension.json` | `claude/.claude-plugin/plugin.json` | None |
| Agents | `agents/*.md` | `claude/agents/*.md` | None — separate dirs |
| Commands | `commands/maestro/*.toml` | `claude/commands/*.md` | None — separate dirs |
| Skills | `skills/*/SKILL.md` | `claude/skills/*/SKILL.md` | None — separate dirs |
| Hooks | `hooks/hooks.json` | `claude/hooks/hooks.json` | None — separate dirs |
| Orchestrator | `GEMINI.md` | `claude/commands/maestro-orchestrate.md` | None — different format |
| Policies | `policies/maestro.toml` | `claude/scripts/policy-enforcer.js` | None — different mechanism |

### Source

All files under `claude/` are copied from overture's `dist/claude-plugin/` output at commit `b314d9d`. The generated output works as-is because `claude/scripts/*.js` require from `../lib/` which resolves to `claude/lib/` (the full copy). Two files in `claude/lib/` receive targeted patches for Claude Code env var fallbacks (see Modifications to Existing Files).

### Duplication Inventory

| Directory | Files | Change Frequency | Risk | Mitigation |
|-----------|-------|-----------------|------|------------|
| `claude/templates/` | 3 Markdown templates | Rarely | Low | CI diff check |
| `claude/references/` | 1 Markdown doc | Rarely | Low | CI diff check |
| `claude/mcp/` | 1 bundled file | Only on MCP rebuild | Low | Phase B eliminates this |
| `claude/lib/` | 14 JS files | Active development | Medium | CI diff check (must account for the 2 patched files) |

CI guard (temporary — removed in Phase C). Excludes the 2 patched files and checks them separately:
```bash
diff -rq lib/ claude/lib/ \
  --exclude=setting-resolver.js \
  --exclude=project-root-resolver.js \
  || (echo "DRIFT: unpatched lib/ files diverged" && exit 1)

# Verify patched files still contain their base logic (diff ignoring added fallback lines)
grep -q "MAESTRO_EXTENSION_PATH" claude/lib/config/setting-resolver.js \
  && grep -q "CLAUDE_PLUGIN_ROOT" claude/lib/config/setting-resolver.js \
  || (echo "DRIFT: setting-resolver.js missing expected env vars" && exit 1)
```

### Modifications to Existing Files

Phase A is predominantly additive. The only modifications:

| File | Change | Reason |
|------|--------|--------|
| `.geminiignore` | Add `claude/` | Prevent Gemini CLI from indexing Claude-specific content into its context window |
| `lib/config/setting-resolver.js` | Add `CLAUDE_PLUGIN_ROOT` fallback for `MAESTRO_EXTENSION_PATH` | Settings resolution under Claude Code AND MCP bundle (see Phase B MCP Environment Variable Resolution) |
| `lib/core/project-root-resolver.js` | Add `CLAUDE_PROJECT_DIR` fallback for `MAESTRO_WORKSPACE_PATH` | Project root detection under Claude Code AND MCP bundle |

The lib patches are applied to the **root** `lib/` (not `claude/lib/`) so that the MCP server bundle (built from root `lib/` via esbuild) also picks them up. The fallback env vars are harmless under Gemini CLI — they simply check additional env vars that are always undefined. The `claude/lib/` copy inherits these patches automatically since it is copied from root `lib/` after patching.

`package.json` `files` array is intentionally NOT updated — the npm package is for Gemini extension distribution only; Claude plugin distribution uses a separate mechanism.

### Installation

**Gemini CLI** (unchanged):
```bash
gemini extensions install https://github.com/josstei/maestro-gemini
```

**Claude Code** (new):
Register the `claude/` subdirectory as a Claude Code plugin. The exact installation mechanism depends on Claude Code's plugin registration (manual `installed_plugins.json` entry or future `claude plugins install` command). The plugin root path must resolve to `<repo>/claude/`. MCP server is configured separately — copy `claude/mcp-config.example.json` into the user's Claude Code MCP settings, adjusting the `cwd` to the repo root.

### Validation Criteria

1. `gemini extensions list` still shows maestro (no Gemini regression)
2. Claude Code discovers 22 agents from `claude/agents/`
3. Claude Code discovers 19 skills from `claude/skills/`
4. Hook scripts execute without `MODULE_NOT_FOUND` errors
5. `/maestro-orchestrate` command activates in Claude Code
6. MCP server starts and responds to `get_session_status`
7. `policy-enforcer.js` blocks `rm -rf` via PreToolUse hook (exits with code 2, returns `permissionDecision: "deny"`)
8. Claude lib env var fallbacks resolve correctly (`CLAUDE_PLUGIN_ROOT` for extension path, `CLAUDE_PROJECT_DIR` for workspace path)

### Rollback

```bash
rm -rf claude/
```

Zero impact on Gemini functionality.

---

## Phase B: MCP Server Source Migration

### Objective

Move the unbundled MCP server source from overture into this repo. Add a build step that produces the bundled output. Both runtimes reference the same built artifact — eliminating the `claude/mcp/` duplication from Phase A.

### MCP Source (from overture `plugins/maestro/src/mcp/`)

The MCP source is larger than a simple server with 4 handlers. The actual tree at overture commit `b314d9d` (and likely expanded further on overture's `main` branch) contains ~32 files across 4 subdirectories. The implementation plan must enumerate the exact file tree at the time of migration — do NOT rely on this spec's summary as a complete listing.

Key structural components:
- `maestro-server.js` — server bootstrap, tool registration, dispatch
- `roots-discovery.js` — workspace root resolution (MCP roots/list → env → git → cwd)
- `runtime-adapter.js` — dev mode vs bundle mode switching (uses `__OVERTURE_BUNDLE__` define)
- `handlers/` — tool handler implementations (session state, workspace, complexity, validation, settings, delegation context, workflow kernel, etc.)
- `lib/` — MCP-internal utilities (session mutations, workflow contract, recovery hints, action gate, etc.)
- `content/` — canonical content providers (agent, skill, template, reference providers with caching)

Dependencies bundled by esbuild: `@modelcontextprotocol/sdk`, `zod`, `js-yaml`, and imports from the repo's shared `lib/state/` and `lib/core/`.

### Changes

**New files:**

| File | Source |
|------|--------|
| `src/mcp/**` (~32 files) | Entire overture `plugins/maestro/src/mcp/` tree — enumerate exact contents during implementation |

**Modified files:**

| File | Change |
|------|--------|
| `package.json` | Add `devDependencies` (`esbuild`, `@modelcontextprotocol/sdk`, `zod`, `js-yaml`), add `scripts.build:mcp` |
| `claude/mcp-config.example.json` | Update path to `../mcp/maestro-server.js` (points to repo root) |

**Deleted files:**

| File | Reason |
|------|--------|
| `claude/mcp/maestro-server.js` | Both runtimes now share `mcp/maestro-server.js` at repo root |

**Build command:**
```json
{
  "scripts": {
    "build:mcp": "esbuild src/mcp/maestro-server.js --bundle --platform=node --target=node18 --format=cjs --define:__OVERTURE_BUNDLE__=true --outfile=mcp/maestro-server.js"
  }
}
```

The `--define:__OVERTURE_BUNDLE__=true` flag is required — `runtime-adapter.js` uses it to switch between dev mode (reads canonical files from disk) and bundle mode (uses inlined content). Without it, the bundled server will fail to resolve canonical content paths at runtime. If the runtime adapter is removed or simplified during migration, this flag can be dropped.

### Import Path Adjustments

MCP source files import from multiple depths. All paths resolve at esbuild bundle time — none appear in the output.

| Import type | Overture pattern | This repo pattern | Notes |
|-------------|-----------------|-------------------|-------|
| Handlers → shared lib | `../../lib/state/...` | `../../../lib/state/...` | One extra `../` due to deeper nesting |
| Server root → shared lib | `../../lib/core/...` | `../../lib/core/...` | Same depth (src/mcp/ → lib/) |
| Handlers → MCP lib | `../lib/workflow-kernel` | `../lib/workflow-kernel` | Intra-MCP, no change needed |
| Handlers → roots-discovery | `../roots-discovery` | `../roots-discovery` | Intra-MCP, no change needed |

The implementation plan must enumerate ALL import paths across the ~32 source files. Additional shared lib files may need to be migrated — e.g., overture has `plugins/maestro/src/lib/plan-graph.js` which the MCP handlers import but which does NOT exist in maestro-gemini's `lib/`. Any such files must be copied into `lib/` during Phase B.

### MCP Environment Variable Resolution

The MCP server bundles its own copy of `lib/core/project-root-resolver.js` via esbuild. Phase A patches `claude/lib/core/project-root-resolver.js` with `CLAUDE_PROJECT_DIR` fallback, but the MCP bundle uses the **root** `lib/` copy (unpatched). This means under Claude Code, the MCP server will not resolve `MAESTRO_WORKSPACE_PATH` via `CLAUDE_PROJECT_DIR`.

**Fix**: Apply the `CLAUDE_PROJECT_DIR` and `CLAUDE_PLUGIN_ROOT` fallback patches to the **root** `lib/` files (not just `claude/lib/`). These env vars simply will not exist under Gemini CLI, so the fallback is harmless — it checks additional env vars that are always undefined on Gemini. This also means Phase A's `claude/lib/` copy inherits the patches automatically instead of requiring separate patching.

### MCP Registration (both runtimes, single artifact)

**Gemini CLI** (`gemini-extension.json` — unchanged):
```json
{ "mcpServers": { "maestro": { "command": "node", "args": ["${extensionPath}/mcp/maestro-server.js"] } } }
```

**Claude Code** (`claude/mcp-config.example.json` — updated):
```json
{ "maestro": { "command": "node", "args": ["mcp/maestro-server.js"], "cwd": "<path-to-repo-root>" } }
```

### Bundled Output: Committed

Both runtimes install without a build step. Users never run `npm run build:mcp`. The build command is for developers modifying MCP source.

### Validation Criteria

1. `npm run build:mcp` produces `mcp/maestro-server.js` with identical tool surface
2. Gemini MCP tools (`resolve_settings`, `get_session_status`, etc.) respond correctly
3. Claude MCP connection works with updated config path
4. No runtime `require` of unbundled dependencies (all inlined by esbuild)
5. `claude/mcp/` directory no longer exists

### Rollback

Revert `package.json`, delete `src/mcp/`, restore `claude/mcp/` from overture dist. The committed `mcp/maestro-server.js` was never overwritten — same file.

---

## Phase C: Lightweight Generator + Rebrand

### Objective

Eliminate all content duplication between runtimes by extracting canonical content and building a lightweight in-repo generator. Rebrand from `maestro-gemini` to `maestro-orchestrate`.

### Problem

After Phase A+B, duplicated content:

| Content | Gemini copy | Claude copy | Files |
|---------|------------|-------------|-------|
| Agents (persona/methodology) | `agents/` (22) | `claude/agents/` (22) | 22 pairs |
| Skills (core) | `skills/` (7) | `claude/skills/` (7 shared + 11 Claude-only) | 7 pairs |
| Lib | `lib/` (14) | `claude/lib/` (14, identical except 2 patched) | 14 files |
| Templates | `templates/` (3) | `claude/templates/` (3, identical) | 3 identical |
| References | `references/` (1) | `claude/references/` (1, identical) | 1 identical |

Agent bodies are ~95% identical between runtimes. Differences are mechanical: tool names, path variables, frontmatter schema, filename casing.

### Generator Scope

**Does:**
- Token replacement: `{{tool:file_read}}` → `read_file` (Gemini) / `Read` (Claude)
- Path replacement: `{{path:plugin_root}}` → `${extensionPath}` / `${CLAUDE_PLUGIN_ROOT}`
- Frontmatter injection: canonical base merged with runtime-specific overrides
- Filename transformation: snake_case (Gemini) / kebab-case (Claude)
- Copy shared files (`lib/`, `templates/`, `references/`, `mcp/`, `scripts/`) into each output

**Does not (scope guard — if the script exceeds 500 lines including `js-yaml` usage, stop and reassess):**
- Body assembly with injection ordering
- Template inclusion (`{{include: ...}}`)
- Cross-plugin support
- Emitter abstraction layer
- Vocabulary validation

**Dependencies:** `js-yaml` for frontmatter parsing/serialization. Frontmatter contains multi-line strings, arrays, and nested objects that cannot be reliably parsed with regex. The line count cap applies to `generate.js` only, not the library.

### Content That Is NOT Canonical

The following content is fundamentally different between runtimes and lives exclusively in `runtimes/<runtime>/entry-points/` — the generator copies them as-is with no token replacement:

- **Orchestrator**: `GEMINI.md` (409 lines, Gemini-specific tool interactions, path variables, constraints, delegation via native subagent calls) vs `maestro-orchestrate.md` + `maestro-orchestrate-skill.md` (Claude-specific tool names, `Agent` tool delegation, `AskUserQuestion` interactions). These are structurally incompatible documents — not candidates for token replacement.
- **11 command-backed skills**: `maestro-review/SKILL.md`, `maestro-debug/SKILL.md`, etc. These exist in Claude only (Gemini uses TOML commands + skill activation instead). They live in `runtimes/claude/entry-points/` and are copied directly to `dist/claude/skills/`.
- **Gemini TOML commands**: 12 TOML files in `runtimes/gemini/entry-points/` — no Claude equivalent (Claude uses Markdown commands).
- **Hook scripts**: `claude/scripts/*.js` have Claude-specific I/O adapters; `hooks/*.js` (Gemini) have Gemini-specific adapters. Both live in their respective `runtimes/<runtime>/` directories.

### Canonical Content Layout

```
maestro-orchestrate/
├── canonical/
│   ├── agents/                          # 22 agent files with {{tool:*}} and {{path:*}} tokens
│   ├── skills/                          # 7 core skills with tokens
│   └── orchestrator/                    # Shared orchestrator workflow logic
├── runtimes/
│   ├── gemini/
│   │   ├── name-map.json               # Token → Gemini names
│   │   ├── agents/*.json               # Per-agent frontmatter overrides
│   │   ├── entry-points/               # TOML commands, GEMINI.md
│   │   └── hooks.json
│   └── claude/
│       ├── name-map.json               # Token → Claude names
│       ├── agents/*.json               # Per-agent frontmatter overrides
│       ├── entry-points/               # Markdown commands, command-backed skills
│       └── hooks.json
├── generator/
│   └── generate.js                     # ~200-300 lines
├── lib/                                # Shared library
├── src/mcp/                            # MCP source (from Phase B)
├── mcp/                                # Built MCP output
├── scripts/                            # Shared utility scripts
├── templates/                          # Shared document templates
└── references/                         # Shared architecture docs
```

### Name Map Format

Each runtime has a `name-map.json` with four sections. The full token set is derived from overture's `vocabulary.json` (22 operations, 5 paths) and `name-resolutions.json`.

**`tools`**: Maps abstract tool tokens to runtime-specific names. All 22 operations from overture's vocabulary must be covered. Example (Claude, showing representative entries — full map defined during implementation):
```json
{
  "file_read": "Read",
  "file_write": "Write",
  "file_edit": "Edit",
  "shell_exec": "Bash",
  "user_prompt": "AskUserQuestion",
  "skill_activate": "Skill",
  "subagent_dispatch": "Agent",
  "plan_enter": "EnterPlanMode",
  "plan_exit": "ExitPlanMode",
  "todo_sync": "TaskCreate"
}
```

Some vocabulary entries map to compound alternatives (e.g., `codebase_search` → `Grep` or `Glob` depending on context). For these, the token replacement uses the primary tool name and a comment in the canonical content clarifies alternatives where needed.

**`paths`**: Maps abstract path tokens to runtime-specific variables.
```json
{
  "plugin_root": "${CLAUDE_PLUGIN_ROOT}",
  "state_dir": "docs/maestro",
  "config_home": "~/.claude"
}
```

**`agents`**: Maps canonical agent names (snake_case) to runtime-specific names. Needed for cross-references in body text (e.g., "delegate to `{{agent:code_reviewer}}`").
```json
{
  "code_reviewer": "code-reviewer",
  "api_designer": "api-designer"
}
```
Single-word agent names (e.g., `coder`, `architect`) are identical across runtimes and do not need entries.

**`naming`**: Controls filename and format conventions.
```json
{
  "agent_filename": "kebab-case",
  "command_format": "md"
}
```

### Canonical Agent Example

```markdown
---
name: coder
description: |
  Implementation specialist for writing clean, well-structured code...
---

You are a **Senior Software Engineer**...

**Methodology:**
- Use {{tool:file_read}} to read existing code before writing
- Use {{tool:file_write}} and {{tool:file_edit}} for modifications
- Run validation via {{tool:shell_exec}}
```

The generator reads this, loads the runtime's `agents/coder.json` override (tools array, model, temperature, etc.), merges frontmatter, replaces tokens, transforms filename, and writes the output.

### De-Tokenization Strategy (Creating Canonical Content)

When extracting canonical content from existing runtime-specific files, tool/path/agent name replacement must be precise to avoid corrupting natural language prose. Rules:

1. **Only replace tokens that appear inside backticks** (`` `read_file` `` → `` `{{tool:file_read}}` ``) or in structured contexts (frontmatter arrays, YAML values, code blocks).
2. **Never replace bare English words** — `Read existing code` stays as-is; only `` `Read` `` (backtick-delimited tool reference) becomes `` `{{tool:file_read}}` ``.
3. **Path variables are unambiguous** — `${extensionPath}` and `${CLAUDE_PLUGIN_ROOT}` do not appear as natural language.
4. **Agent names in delegation contexts** — `delegate to code_reviewer` → `delegate to {{agent:code_reviewer}}`.
5. **Use the Claude version as the canonical base** (Claude tool names like `Read`, `Write` are more likely to conflict with prose; starting from Claude and replacing backtick-delimited instances ensures correctness). Cross-check against the Gemini version for any missed tokens.

### Generator Algorithm

```
1. Parse --runtime argument (gemini | claude | both)
2. Load name-map.json for target runtime(s)
3. For each agent in canonical/agents/:
   a. Parse frontmatter + body (using js-yaml)
   b. Load runtime override from runtimes/<runtime>/agents/<name>.json
   c. Merge: canonical frontmatter ← runtime override (override wins)
   d. Replace all {{tool:*}}, {{path:*}}, and {{agent:*}} tokens in body
   e. Transform filename per naming.agent_filename
   f. Write to dist/<runtime>/agents/
4. For each skill in canonical/skills/:
   a. RECURSIVELY read all .md files in skill directory tree (e.g., delegation/protocols/*.md)
   b. Replace tokens in each .md
   c. Write to dist/<runtime>/skills/ preserving directory structure
5. Copy runtime-specific entry-points from runtimes/<runtime>/entry-points/ to appropriate locations:
   - Gemini: TOML commands → dist/gemini/commands/maestro/, GEMINI.md → dist/gemini/
   - Claude: Markdown commands → dist/claude/commands/, command-backed skills → dist/claude/skills/
6. Copy runtime-specific hook scripts from runtimes/<runtime>/scripts/ to dist/<runtime>/scripts/
   (Claude has 10 scripts including hook adapter and policy enforcer; Gemini has 6 hook entry points)
7. Copy hooks config from runtimes/<runtime>/hooks.json to dist/<runtime>/hooks/
8. Copy shared directories into each runtime output: lib/, templates/, references/, mcp/
   - Copy shared utility scripts (ensure-workspace.js, read-*.js, write-state.js) into dist/<runtime>/scripts/
9. Apply runtime-specific lib patches if any exist at runtimes/<runtime>/lib-patches/
   (NOTE: For Phase C, the env var fallback patches should be applied to ROOT lib/ instead — see below)
10. Write runtime manifest (gemini-extension.json or .claude-plugin/plugin.json)
11. Validate: scan dist/ for any remaining {{tool:*}}, {{path:*}}, or {{agent:*}} tokens — fail if found
```

### Lib Patch Survival Through Phase C

Phase A applies env var fallback patches to root `lib/` (not `claude/lib/`). In Phase C, the generator copies root `lib/` into both runtime outputs. Since the patches are in root `lib/`, they automatically propagate to `dist/gemini/lib/` and `dist/claude/lib/`. The fallback env vars are harmless under Gemini (always undefined). No additional patch mechanism is needed.

### Generated Output

`npm run generate` produces `dist/gemini/` and `dist/claude/`, each a complete self-contained runtime package.

### `dist/` Commit Strategy

`dist/` is **committed to git** (remove `dist/` from `.gitignore`). Rationale:
- Users install without a build step (`gemini extensions install` or Claude plugin registration)
- GitHub releases reference repo content directly
- CI regenerates and verifies `dist/` matches — fails if stale

To prevent direct edits to generated output, CI validates that `npm run generate` produces no diff against committed `dist/`.

### Installation Migration (Breaking Change)

Phase C changes the Gemini installation path from `gemini extensions link .` (repo root) to `gemini extensions link dist/gemini/` (generated output). This is a **breaking change** for existing users.

Migration strategy:
1. The GitHub repo rename (`maestro-gemini` → `maestro-orchestrate`) already forces users to update their install command (GitHub auto-redirects the old URL, but `gemini extensions install` may not follow redirects)
2. `README.md` documents the new installation command prominently
3. `CHANGELOG.md` includes a "Breaking Changes" section with migration instructions
4. The previous release tag (`v1.4.0`) remains installable from the old URL for users who pin versions
5. Add a minimal `gemini-extension.json` stub at repo root that loads a single-agent extension whose description says "Maestro has moved — run `gemini extensions link dist/gemini/` instead". This gives users a visible message when they `gemini extensions link .` instead of a silent failure or cryptic parse error

### Rebrand

| File | Change |
|------|--------|
| `package.json` | `name` → `@maestro-orchestrator/maestro-orchestrate` |
| `gemini-extension.json` | `name` stays `maestro` (user-facing name) |
| `.claude-plugin/plugin.json` | `name` stays `maestro` (user-facing name) |
| `README.md` | Title, description, installation URLs |
| `CHANGELOG.md` | New entry documenting rebrand |
| GitHub repo | Rename `maestro-gemini` → `maestro-orchestrate` |

### Files Deleted or Moved (relative to Phase A+B)

| Path | Destination | Reason |
|------|-------------|--------|
| `agents/` | `canonical/agents/` (de-tokenized) | Replaced by canonical + generated output |
| `claude/` (entire directory) | Generated `dist/claude/` | Replaced by generated output |
| `skills/` | `canonical/skills/` (de-tokenized) | Replaced by canonical + generated output |
| `commands/maestro/*.toml` | `runtimes/gemini/entry-points/` | Gemini-specific entry points |
| `hooks/hooks.json` | `runtimes/gemini/hooks.json` | Gemini hook config |
| `hooks/*.js` (5 hook entry points + adapter) | `runtimes/gemini/scripts/` | Gemini hook scripts |
| `GEMINI.md` | `runtimes/gemini/entry-points/` | Gemini orchestrator (non-canonical) |
| `policies/maestro.toml` | `runtimes/gemini/entry-points/` | Gemini policy file |
| `.gitignore` entry for `dist/` | Removed | `dist/` is now committed |

Files that remain at repo root unchanged: `lib/`, `src/mcp/`, `mcp/`, `scripts/` (shared utility scripts only), `templates/`, `references/`, `package.json`, `README.md`, `CHANGELOG.md`, `LICENSE`, `site/`, `.github/`.

### Maintenance Considerations

- **Agent renames**: Both orchestrator files (`runtimes/gemini/entry-points/GEMINI.md` and `runtimes/claude/entry-points/maestro-orchestrate.md`) must be updated manually when agents are added, removed, or renamed.
- **Concurrent sessions**: Both runtimes default to `docs/maestro/` for session state. Simultaneous Maestro sessions from Gemini CLI and Claude Code on the same project will corrupt `active-session.md`. This is an unsupported configuration — document it in README.

### New Scripts

```json
{
  "scripts": {
    "generate": "node generator/generate.js",
    "generate:gemini": "node generator/generate.js --runtime gemini",
    "generate:claude": "node generator/generate.js --runtime claude",
    "build:mcp": "esbuild src/mcp/maestro-server.js --bundle --platform=node --target=node18 --format=cjs --outfile=mcp/maestro-server.js",
    "build": "npm run build:mcp && npm run generate"
  }
}
```

### Development Workflow

```bash
vim canonical/agents/coder.md      # Edit canonical content once
npm run generate                    # Produce both runtimes
gemini extensions link dist/gemini/ # Test Gemini
                                    # Point Claude plugin to dist/claude/
```

### Validation Criteria

1. `npm run generate` produces `dist/gemini/` and `dist/claude/` with all expected files
2. Generated Gemini output is functionally equivalent to pre-Phase-C state
3. Generated Claude output is functionally equivalent to pre-Phase-C state
4. Zero `{{tool:*}}` or `{{path:*}}` tokens remain in generated output
5. CI runs `npm run generate` and validates no token leaks
6. Generator script (`generate.js`) is under 500 lines excluding `js-yaml` library (scope guard)
7. Both runtimes load and function correctly from generated output

### Rollback

Delete `canonical/`, `runtimes/`, `generator/`. Restore Phase A+B static files from git history. The static dual-runtime repo remains fully functional.

---

## Phase Ordering and Dependencies

```
Phase A ──→ Phase B ──→ Phase C
(additive)   (MCP)      (generator + rebrand)
```

- **A → B**: Phase B deletes `claude/mcp/` (added in Phase A). Phase B requires Phase A to be complete so the Claude plugin exists to update.
- **B → C**: Phase C restructures everything. It needs the MCP build pipeline from Phase B in place.
- Each phase produces a shippable, rollback-safe state.

## Risk Summary

| Risk | Phase | Likelihood | Impact | Mitigation |
|------|-------|-----------|--------|------------|
| Claude hook contract mismatch | A | Medium | High | Validate hook I/O against live Claude Code session; overture's adapter tested at commit b314d9d |
| Content drift between runtimes | A | Medium | Medium | CI diff check on shared lib/, templates/, references/ (excluding patched files) |
| MCP source tree larger than expected | B | High | Medium | Enumerate full file tree from overture during implementation; spec's summary is not exhaustive |
| MCP import paths and missing shared lib files | B | Medium | High | Audit ALL imports; migrate any shared lib files (e.g., plan-graph.js) that exist in overture but not here |
| MCP `__OVERTURE_BUNDLE__` flag and runtime adapter | B | Medium | High | Include `--define` in esbuild command; decide whether to keep or remove runtime adapter |
| MCP path confusion (plugin root vs cwd) | B | Medium | Low | Documentation clearly states MCP `cwd` MUST be repo root, not claude/ |
| De-tokenization corrupts prose | C | Medium | High | Only replace backtick-delimited tool names and structured contexts; use Claude version as base |
| Compound tool name tokens (1:many mappings) | C | Medium | Medium | Use primary tool name; annotate alternatives in canonical content comments |
| Generator scope creep toward overture | C | Medium | High | Hard 500-line cap (excluding js-yaml); stop and reassess if exceeded |
| Gemini installation breaking change | C | High | High | Migration docs, CHANGELOG, stub gemini-extension.json with redirect message |
| Concurrent sessions corrupt shared state | C | Low | High | Document as unsupported; both runtimes write to same `docs/maestro/` |
| Cross-platform path issues | All | Low | Medium | No symlinks; all paths are standard relative paths |

## Out of Scope

- Codex runtime support (can be added as a third runtime in Phase C's framework later)
- Overture decommission process (separate task after Phase C ships)
- Claude Code MCP auto-discovery (not supported by Claude Code plugin system — manual config required)
- New agents, skills, or workflow changes (this spec is purely structural)
- Documentation site (`site/`) updates — content references to repo name can be updated as a follow-up after Phase C rebrand
- Claude Code plugin marketplace publishing (manual registration only for now)
