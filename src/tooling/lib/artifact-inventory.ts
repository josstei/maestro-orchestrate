import {
  ARTIFACT_INVENTORY,
  ARTIFACT_SCOPE,
  RUNTIME_DIST_PATHS,
  npmFiles,
  releasePaths,
} from '../artifact-policy.js';

const SCOPE = ARTIFACT_SCOPE;
const INVENTORY = ARTIFACT_INVENTORY;

export { SCOPE, INVENTORY, RUNTIME_DIST_PATHS, npmFiles, releasePaths };
